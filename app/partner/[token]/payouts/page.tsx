import { notFound } from "next/navigation";
import { getPartnerPortal } from "../../../../db/partner";
import { money, shortDate } from "../../_lib";

const rewardStatus: Record<string, string> = { PENDING: "Ожидается", APPROVED: "К выплате", PAID: "Выплачено", CANCELLED: "Отменено" };

export default async function PartnerPayoutsPage({ params }: { params: Promise<{ token: string }> }) { const { token } = await params; const portal = await getPartnerPortal(token); if (!portal) notFound(); const approved = portal.rewards.filter((item) => item.status === "APPROVED").reduce((sum, item) => sum + item.amount, 0); return <div className="partner-portal-content"><div className="partner-page-heading"><div><span>ЖУРНАЛ НАЧИСЛЕНИЙ</span><h1>Выплаты без догадок</h1><p>Основание, связанный лид, плановая дата и отметка компании о ручной выплате.</p></div></div><section className="payout-hero"><small>ДОСТУПНО К ВЫПЛАТЕ</small><strong>{money(approved, portal.program.currency)}</strong><span>Компания переводит деньги вручную</span></section>{portal.rewards.length ? <section className="partner-payout-table"><div><span>ОСНОВАНИЕ</span><span>КОМПАНИЯ</span><span>СУММА</span><span>ПЛАНОВАЯ ДАТА</span><span>СТАТУС</span></div>{portal.rewards.map((reward) => { const submission = portal.submissions.find((item) => item.id === reward.submissionId); return <article key={reward.id}><div><strong>{submission?.mission?.title || "Вознаграждение"}</strong><small>{submission?.contactCompany}</small></div><span>{portal.company.name}</span><b>{money(reward.amount, reward.currency)}</b><span>{shortDate(reward.plannedAt)}</span><em className={`reward-status-${reward.status.toLowerCase()}`}>{rewardStatus[reward.status] ?? reward.status}</em></article>; })}</section> : <section className="partner-large-empty"><span>₸</span><h2>Начислений пока нет</h2><p>После принятия результата компания создаст начисление с плановой датой выплаты.</p></section>}<div className="manual-payment-note"><span>!</span><div><strong>Relay не переводит деньги автоматически</strong><p>Компания платит самостоятельно и отмечает выплату в журнале. Вы всегда видите основание и связанную рекомендацию.</p></div></div></div>; }
