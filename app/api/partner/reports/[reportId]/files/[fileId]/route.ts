import { getPartnerPortal } from "../../../../../../../db/partner";
import { getReportFileForPartner } from "../../../../../../../db/reports";
import { getFilesBucket } from "../../../../../../../lib/storage";
export async function GET(request: Request, { params }: { params: Promise<{ reportId: string; fileId: string }> }) { const { reportId, fileId } = await params; const token = new URL(request.url).searchParams.get("token") || ""; const portal = await getPartnerPortal(token); if (!portal) return new Response("Недоступно", { status: 401 }); const file = await getReportFileForPartner(reportId, fileId, portal.partners.map((item) => item.id)); if (!file) return new Response("Не найдено", { status: 404 }); const object = await getFilesBucket().get(file.objectKey); if (!object) return new Response("Не найдено", { status: 404 }); const inline = new URL(request.url).searchParams.get("inline") === "1"; return new Response(object.body, { headers: { "content-type": file.mimeType, "content-disposition": `${inline ? "inline" : "attachment"}; filename*=UTF-8''${encodeURIComponent(file.fileName)}`, "cache-control": "private, no-store" } }); }
